# วิธีเชื่อมหน้าเว็บกับ Google Sheet (ทำครั้งเดียว ~5 นาที)

หน้า `index.html` จะคุยกับชีตผ่าน **Google Apps Script Web App** ที่ deploy จากชีตเอง
ทำให้ทุกครั้งที่กด “บันทึก” ข้อมูลขึ้นชีตทันที และหน้าเว็บจะดึงข้อมูลจากชีตกลับมาให้อัตโนมัติทุก 15 วินาที

ชีตฐานข้อมูลที่ใช้:
<https://docs.google.com/spreadsheets/d/1YG-HCGAEmsliXe0eReUI52Tdl1bDdPhBwex24DRptl0/edit>

---

## 1) ติดตั้งสคริปต์ในชีต

1. เปิดชีต → เมนู **ส่วนขยาย (Extensions)** → **Apps Script**
2. ลบโค้ดเดิมใน `Code.gs` ทิ้ง แล้ววางเนื้อหาจากไฟล์ [`apps-script/Code.gs`](apps-script/Code.gs) ของ repo นี้ลงไปทั้งหมด
3. เช็คบรรทัดนี้ให้ตรงกับ **ชื่อแท็บที่มีข้อมูลจริง** (ชื่อที่อยู่ล่างสุดของหน้าจอชีต)

   ```js
   var SHEET_NAME = 'Check ม่าน';
   ```

4. (ถ้าต้องการใส่รหัสกันคนอื่นเขียนมั่ว) แก้บรรทัด

   ```js
   var TOKEN = '';        // → var TOKEN = 'รหัสลับที่ตั้งเอง';
   ```

5. กด 💾 บันทึก

> **สคริปต์จะไม่สร้างแท็บใหม่เด็ดขาด** — ถ้าหาแท็บชื่อนี้ไม่เจอจะฟ้อง error พร้อมบอกว่ามีแท็บอะไรบ้าง
> (ถ้ายังมีแท็บ `Curtain Check` ที่สร้างไว้ตอนทดลอง ลบทิ้งได้เลย ไม่มีอะไรอ้างถึงแล้ว)

## 2) Deploy เป็น Web App

1. มุมขวาบน → **Deploy** → **New deployment**
2. ไอคอนเฟือง ⚙️ ข้าง “Select type” → เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** `Me (อีเมลของคุณ)`
   - **Who has access:** `Anyone` ← ต้องเป็นอันนี้ ไม่งั้นหน้าเว็บจะเรียกไม่ได้
4. **Deploy** → กด **Authorize access** → เลือกบัญชี → “Advanced” → “Go to … (unsafe)” → **Allow**
   (ขึ้นเตือนเพราะเป็นสคริปต์ที่เราเขียนเอง ไม่ได้ผ่านการรีวิวของ Google — ปกติ)
5. คัดลอก **Web app URL** ที่ได้ หน้าตาแบบนี้

   ```
   https://script.google.com/macros/s/AKfycb.....xyz/exec
   ```

## 3) บอก URL ให้หน้าเว็บ — เลือกวิธีใดวิธีหนึ่ง

**วิธี ก. ใส่ในโค้ดครั้งเดียว (แนะนำ — ทุกเครื่องที่เปิดลิงก์จะเชื่อมเองเลย)**

แก้ `index.html` บรรทัดนี้ แล้ว commit ขึ้น GitHub

```js
const SHEET_WEBAPP_URL = "";   // ← วาง URL ที่ลงท้าย /exec ตรงนี้
const SHEET_TOKEN      = "";   // ← ถ้าตั้ง TOKEN ไว้ ใส่ให้ตรงกัน
```

**วิธี ข. ตั้งจากหน้าเว็บ (ตั้งแยกทีละเครื่อง)**

แตะปุ่ม **● เชื่อมชีต** บนแถบบนสุด → วาง URL → กด **ทดสอบ** → **บันทึก & ซิงก์**

**วิธี ค. ส่งลิงก์ตั้งค่าให้ทีม**

```
https://<user>.github.io/curtainandsheer/?api=<URL ที่ลงท้าย /exec>&token=<token>
```

เปิดลิงก์นี้ครั้งเดียว เครื่องนั้นจะจำค่าไว้ให้เอง (URL จะถูกล้างออกจากช่อง address อัตโนมัติ)

---

## ใช้งานประจำวัน

| สิ่งที่เกิดขึ้น | ผลกับชีต |
|---|---|
| กด **บันทึก / อัปเดต** ห้องหนึ่ง | เขียนแถวนั้นขึ้นชีตทันที |
| มีคนอื่นแก้ชีต หรือบันทึกจากมือถืออีกเครื่อง | หน้าเว็บดึงมาให้เองภายใน 15 วินาที (และทุกครั้งที่สลับกลับมาที่แท็บนี้) |
| เน็ตหลุด | ยังบันทึกได้ตามปกติ ข้อมูลเก็บไว้ในเครื่องและเข้าคิว — ป้ายบนแถบจะขึ้น “ค้าง n” แล้วส่งให้เองเมื่อเน็ตกลับมา |
| ปุ่ม **↑ ส่งขึ้นชีตทั้งหมด** | ยัดทุกห้องในเครื่องขึ้นชีตอีกรอบ (ใช้ตอนเพิ่งเชื่อมครั้งแรก) |
| ปุ่ม **↓ ดึงจากชีต** | ดึงข้อมูลล่าสุดมาทับทันที ไม่ต้องรอ 15 วินาที |
| ปุ่ม **ล้างข้อมูลในเครื่องนี้** | ล้างเฉพาะในมือถือเครื่องนั้น — **ชีตไม่ถูกแตะเลย** กด “↓ ดึงจากชีต” เอากลับมาได้ทันที |

> ไม่มีปุ่มไหนในแอปที่ลบข้อมูลในชีตได้ ถ้าต้องการลบต้องไปลบในชีตเอง

### สถานะบนแถบบนสุด

| ป้าย | ความหมาย |
|---|---|
| ● เชื่อมชีต (เทา) | ยังไม่ได้ตั้งค่า — แตะเพื่อใส่ URL |
| ● กำลังซิงก์ (เหลือง) | กำลังส่ง/ดึงข้อมูล |
| ● ชีตซิงก์แล้ว (เขียว) | ตรงกันกับชีตเรียบร้อย |
| ● ค้าง n / ต่อไม่ติด (แดง) | ส่งไม่ได้ตอนนี้ — เก็บไว้ในเครื่องก่อน แตะเพื่อดูสาเหตุ |

---

## กติกาการเก็บข้อมูล

- เขียนลง **แท็บเดิม ต่อท้ายข้อมูลเก่า** เท่านั้น ไม่สร้างแท็บใหม่ ไม่ย้ายข้อมูลเดิม
- **หนึ่งแถว = หนึ่งห้อง ต่อหนึ่งวันตรวจ** — คีย์คือ `วันที่ตรวจ + Room No.`
  ตรวจห้องเดิมซ้ำในวันเดียวกัน = ทับแถวเดิม, ตรวจคนละวัน = ต่อแถวใหม่ (แถวเก่ายังอยู่ครบ เป็นประวัติ)
- หน้าเว็บแสดง **รายการล่าสุดของแต่ละห้อง** (ดูจากวันที่ตรวจ)
- แอปดูแลคอลัมน์ **A–S** เท่านั้น คอลัมน์ถัดจากนั้น (เช่น `สถานะ`) เป็นของคุณล้วน ๆ แอปไม่แตะ
- **ช่องที่เป็นสูตรจะไม่ถูกเขียนทับ** เช่นถ้า `ผลตรวจ` เป็นสูตร แอปจะปล่อยให้สูตรทำงานต่อ
- แถวใหม่ที่แอปเพิ่ม จะก๊อป **สี / dropdown / สูตร** จากแถวสุดท้ายมาให้อัตโนมัติ หน้าตาเหมือนแถวเดิม
- แก้ค่าในชีตตรง ๆ ได้เลย เดี๋ยวแอปดึงมาแสดงเอง — แต่อย่าสลับลำดับคอลัมน์ A–S

## ถ้าเชื่อมไม่ได้

| อาการ | สาเหตุที่เจอบ่อย |
|---|---|
| กด “ทดสอบ” แล้วขึ้น `Failed to fetch` | ตอน deploy ไม่ได้เลือก **Who has access: Anyone** |
| ขึ้น `ไม่พบแท็บชื่อ …` | `SHEET_NAME` ไม่ตรงกับชื่อแท็บ — ในข้อความ error จะบอกชื่อแท็บที่มีอยู่จริงให้เลย ก๊อปไปวางได้เลย |
| ขึ้น `unauthorized` | `TOKEN` ใน Apps Script กับช่อง Token ในแอปไม่ตรงกัน |
| แก้โค้ดใน Apps Script แล้วไม่มีผล | ต้อง **Deploy → Manage deployments → ✏️ → Version: New version → Deploy** ทุกครั้ง (URL เดิมใช้ต่อได้) |
| อยากเช็คว่าสคริปต์อ่านแท็บไหนอยู่ | ในชีตมีเมนู **Curtain Check → เช็คว่าสคริปต์อ่านแท็บไหน** (ต้อง refresh ชีตหนึ่งครั้งหลังวางโค้ด) |
