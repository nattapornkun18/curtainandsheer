# วิธีเชื่อมหน้าเว็บกับ Google Sheet (ทำครั้งเดียว ~5 นาที)

หน้า `index.html` จะคุยกับชีตผ่าน **Google Apps Script Web App** ที่ deploy จากชีตเอง
ทำให้ทุกครั้งที่กด “บันทึก” ข้อมูลขึ้นชีตทันที และหน้าเว็บจะดึงข้อมูลจากชีตกลับมาให้อัตโนมัติทุก 15 วินาที

ชีตฐานข้อมูลที่ใช้:
<https://docs.google.com/spreadsheets/d/1YG-HCGAEmsliXe0eReUI52Tdl1bDdPhBwex24DRptl0/edit>

---

## 1) ติดตั้งสคริปต์ในชีต

1. เปิดชีต → เมนู **ส่วนขยาย (Extensions)** → **Apps Script**
2. ลบโค้ดเดิมใน `Code.gs` ทิ้ง แล้ววางเนื้อหาจากไฟล์ [`apps-script/Code.gs`](apps-script/Code.gs) ของ repo นี้ลงไปทั้งหมด
3. เช็คบรรทัดนี้ให้ตรงกับ **ชื่อแท็บที่มีข้อมูลจริง** (ชื่อที่อยู่ล่างสุดของหน้าจอชีต)

   ```js
   var SHEET_NAME = 'Check ม่าน';
   ```

4. ตั้ง **รหัสผ่าน** ที่ต้องกรอกก่อนเข้าใช้แอป (แนะนำให้ตั้ง)

   ```js
   var TOKEN = '';        // → var TOKEN = '742';
   ```

5. กด 💾 บันทึก

> **สคริปต์จะไม่สร้างแท็บใหม่เด็ดขาด** — ถ้าหาแท็บชื่อนี้ไม่เจอจะฟ้อง error พร้อมบอกว่ามีแท็บอะไรบ้าง
> (ถ้ายังมีแท็บ `Curtain Check` ที่สร้างไว้ตอนทดลอง ลบทิ้งได้เลย ไม่มีอะไรอ้างถึงแล้ว)

## 2) Deploy เป็น Web App

1. มุมขวาบน → **Deploy** → **New deployment**
2. ไอคอนเฟือง ⚙️ ข้าง “Select type” → เลือก **Web app**
3. ตั้งค่า:
   - **Execute as:** `Me (อีเมลของคุณ)`
   - **Who has access:** `Anyone` ← ต้องเป็นอันนี้ ไม่งั้นหน้าเว็บจะเรียกไม่ได้
4. **Deploy** → กด **Authorize access** → เลือกบัญชี → “Advanced” → “Go to … (unsafe)” → **Allow**
   (ขึ้นเตือนเพราะเป็นสคริปต์ที่เราเขียนเอง ไม่ได้ผ่านการรีวิวของ Google — ปกติ)
5. คัดลอก **Web app URL** ที่ได้ หน้าตาแบบนี้

   ```
   https://script.google.com/macros/s/AKfycb.....xyz/exec
   ```

## 3) บอก URL ให้หน้าเว็บ — เลือกวิธีใดวิธีหนึ่ง

**วิธี ก. ใส่ในโค้ดครั้งเดียว (แนะนำ — ทุกเครื่องที่เปิดลิงก์จะเชื่อมเองเลย)**

แก้ `index.html` บรรทัดนี้ แล้ว commit ขึ้น GitHub

```js
const SHEET_WEBAPP_URL = "";   // ← วาง URL ที่ลงท้าย /exec ตรงนี้
const SHEET_TOKEN      = "";   // ← ถ้าตั้ง TOKEN ไว้ ใส่ให้ตรงกัน
```

**วิธี ข. ตั้งจากหน้าเว็บ (ตั้งแยกทีละเครื่อง)**

แตะปุ่ม **● เชื่อมชีต** บนแถบบนสุด → วาง URL → กด **ทดสอบ** → **บันทึก & ซิงก์**

**วิธี ค. ส่งลิงก์ตั้งค่าให้ทีม**

```
https://<user>.github.io/curtainandsheer/?api=<URL ที่ลงท้าย /exec>&token=<token>
```

เปิดลิงก์นี้ครั้งเดียว เครื่องนั้นจะจำค่าไว้ให้เอง (URL จะถูกล้างออกจากช่อง address อัตโนมัติ)

---

## รหัสผ่าน

ตั้งที่ **`TOKEN` ใน Apps Script** ที่เดียว แล้วแอปจะขึ้นหน้าล็อกให้กรอกก่อนใช้งานอัตโนมัติ

```js
var TOKEN = '742';     // ตัวเลขกี่หลักก็ได้
```

ถ้าตั้งยาวไม่เท่ากับ 3 หลัก แก้ `PIN_LEN` ใน `index.html` ให้ตรงกันด้วย (ไว้บอกว่าจอกรอกกี่ช่อง)

```js
const PIN_LEN = 3;
```

**รหัสนี้กันได้จริง ไม่ใช่แค่บังหน้าจอ** — เพราะเป็นรหัสเดียวกับที่ Apps Script ใช้ตรวจทุก request
ใครไม่มีรหัสจะ **อ่านข้อมูลก็ไม่ได้ เขียนก็ไม่ได้** ต่อให้เปิด View Source หรือยิง URL ตรง ๆ ก็ตาม

| เรื่อง | พฤติกรรม |
|---|---|
| กรอกครั้งแรก | ถูกแล้วเครื่องนั้นจำไว้ ไม่ต้องกรอกซ้ำทุกครั้งที่เปิด |
| อยากล็อกเอง | ปุ่ม **🔒 ล็อกหน้าจอ** ในแท็บ “บันทึกแล้ว” |
| เปลี่ยนรหัส | แก้ `TOKEN` แล้ว **Deploy → New version** ทุกเครื่องจะถูกเด้งให้กรอกรหัสใหม่เอง |
| ยังไม่ปลดล็อก | แอปจะไม่ดึงและไม่ส่งข้อมูลใด ๆ เลย |
| ไม่อยากใช้รหัส | เว้น `TOKEN = ''` ไว้ แอปจะไม่ขึ้นหน้าล็อกเลย |

> รหัส 3 หลักมีแค่ 1000 แบบ — กันคนทั่วไปที่บังเอิญเจอลิงก์ได้สบาย
> แต่ถ้ากลัวคนตั้งใจไล่เดา ใช้ 5–6 หลักจะแน่นกว่ามาก (แก้ `TOKEN` กับ `PIN_LEN` ให้ตรงกัน)

---

## แจ้งเตือนเข้า LINE

ค่าเริ่มต้นคือ **แจ้งเฉพาะตอนแก้ไขห้องที่เคยบันทึกไปแล้ว** และบอกด้วยว่าเปลี่ยนจากอะไรเป็นอะไร

```
✏️ แก้ไข 310 · deluxe king
🟢 ผลตรวจ: DEFECT → PASS
• เสียง ม่านโปร่ง: bad → good
📝 รางฝืด → เปลี่ยนรางใหม่แล้ว
— ตุ๊ก · 29/07/2026 (ตรวจครั้งก่อน 23/07/2026)
```

**อะไรถือว่าเป็น “การแก้ไข”**

| การกระทำ | แจ้งไหม |
|---|---|
| ตรวจห้องที่ยังไม่เคยตรวจ (แถว `รอตรวจ` ที่ยังไม่มีวันที่) | ❌ ไม่แจ้ง — เป็นการตรวจครั้งแรก |
| เพิ่มห้องใหม่ที่ยังไม่มีในตาราง | ❌ ไม่แจ้ง |
| ตรวจซ้ำห้องที่เคยบันทึกแล้ว **แล้วมีค่าเปลี่ยน** | ✅ แจ้ง พร้อมบอกจุดที่เปลี่ยน |
| ตรวจซ้ำแล้วผลเหมือนเดิมทุกอย่าง | ❌ ไม่แจ้ง — กันข้อความกวน |
| แก้เฉพาะหมายเหตุ | ✅ แจ้ง |

แก้หลายห้องพร้อมกันจะยุบเหลือข้อความสรุปอันเดียว ไม่รัวเป็นสิบข้อความ

### ตั้งค่า LINE OA (ทำครั้งเดียว ~10 นาที)

> **LINE Notify ปิดบริการไปแล้วตั้งแต่ มี.ค. 2568** ต้องใช้ LINE OA + Messaging API แทน

**ถ้ายังไม่มี LINE OA** — สร้างใหม่ผ่าน LINE Developers แล้วระบบจะสร้าง OA ให้เองอัตโนมัติ

1. เข้า <https://developers.line.biz/console/> ล็อกอินด้วยบัญชี LINE
2. **Create a new provider** (ตั้งชื่ออะไรก็ได้ เช่นชื่อโรงแรม) → มีอยู่แล้วข้ามได้
3. ในโปรไวเดอร์นั้น → **Create a new channel** → เลือก **Messaging API**
   กรอกชื่อ (เช่น “แจ้งเตือนเช็คม่าน”) หมวดหมู่ ประเทศไทย → **Create**
   → ตอนนี้คุณมี LINE OA เรียบร้อยแล้ว มันจะไปโผล่ใน LINE Official Account Manager เอง

**ถ้ามี LINE OA ของโรงแรมอยู่แล้ว และอยากใช้ตัวเดิม**

1. เข้า <https://manager.line.biz/> เลือกบัญชีนั้น
2. **การตั้งค่า → Messaging API → เปิดใช้ Messaging API**
3. เลือก provider (หรือสร้างใหม่) → ยืนยัน
4. จากนั้นบัญชีนี้จะไปโผล่ใน LINE Developers Console ให้ไปเอา token ในขั้นตอนถัดไป

> ถ้า OA เดิมใช้คุยกับลูกค้าอยู่ **แนะนำให้สร้างตัวใหม่แยกต่างหาก** จะได้ไม่ปนกับงานลูกค้า
> และไม่ไปกินโควตาข้อความของงานขาย

**เอา token มาใส่ (ทำเหมือนกันทั้งสองแบบ)**

5. ใน LINE Developers เปิดแท็บ **Messaging API** เลื่อนลงล่างสุด
   หัวข้อ **Channel access token (long-lived)** → **Issue** → คัดลอกไว้
6. หน้าเดียวกันมี **QR code** ของบัญชีนี้ — ให้ทุกคนที่อยากรับแจ้งเตือน **สแกนเพิ่มเป็นเพื่อน**
7. ที่ <https://manager.line.biz/> → **การตั้งค่า → การตอบกลับ** → **ปิด** ข้อความตอบกลับอัตโนมัติและข้อความทักทาย
   (ไม่งั้นบอทจะเด้งข้อความอัตโนมัติมากวน)
8. วาง token ใน `Code.gs`

   ```js
   var LINE_TOKEN = 'วาง token ตรงนี้';
   ```

9. **Deploy → Manage deployments → ✏️ → New version → Deploy**
10. รีเฟรชชีต → เมนู **Curtain Check → ทดสอบส่งแจ้งเตือน LINE** → ต้องเด้งเข้ามือถือ

### ตัวเลือกเพิ่มเติม

```js
var LINE_TO = '';           // เว้นว่าง = ส่งหาทุกคนที่เป็นเพื่อนกับ OA นี้
                            // ใส่ userId/groupId ถ้าอยากส่งเจาะจง
var NOTIFY_WHEN = 'update'; // 'update' = เฉพาะตอนแก้ไขห้องที่เคยบันทึกแล้ว (ค่าเริ่มต้น)
                            // 'defect' = เฉพาะห้องที่ผลมีปัญหา (ตรวจครั้งแรกก็แจ้ง)
                            // 'all'    = ทุกครั้งที่กดบันทึก
                            // 'off'    = ปิดแจ้งเตือน
```

### ⚠️ เรื่องโควตาข้อความ

LINE OA **แผนฟรีส่งได้ 500 ข้อความ/เดือน** และ **นับตามจำนวนคนที่ได้รับ**
(มีคนรับ 3 คน = ส่ง 1 ครั้งนับ 3 ข้อความ)

โหมด `'update'` ที่ตั้งไว้ให้ประหยัดมากอยู่แล้ว เพราะตรวจรอบแรกทั้งตึกไม่ส่งอะไรเลย
จะส่งก็ต่อเมื่อมีการกลับไปแก้ห้องที่เคยบันทึกแล้ว ซึ่งปกติมีไม่กี่ห้องต่อรอบ

ถ้าเปลี่ยนไปใช้ `'all'` ให้ระวัง: ตรวจ 30 ห้อง คนรับ 3 คน = 90 ข้อความ ได้ราว 5 รอบ/เดือน

พอโควตาหมด LINE จะตอบ `429` — **ข้อมูลยังลงชีตครบตามปกติ แค่ไม่มีข้อความเข้า** ไม่มีอะไรพัง

---

## ใช้งานประจำวัน

| สิ่งที่เกิดขึ้น | ผลกับชีต |
|---|---|
| กด **บันทึก / อัปเดต** ห้องหนึ่ง | ทับแถวของห้องนั้นในชีตทันที (ไม่เพิ่มแถวใหม่) |
| มีคนอื่นแก้ชีต หรือบันทึกจากมือถืออีกเครื่อง | หน้าเว็บดึงมาให้เองภายใน 15 วินาที (และทุกครั้งที่สลับกลับมาที่แท็บนี้) |
| เน็ตหลุด | ยังบันทึกได้ตามปกติ ข้อมูลเก็บไว้ในเครื่องและเข้าคิว — ป้ายบนแถบจะขึ้น “ค้าง n” แล้วส่งให้เองเมื่อเน็ตกลับมา |
| ปุ่ม **↑ ส่งขึ้นชีตทั้งหมด** | ยัดทุกห้องในเครื่องขึ้นชีตอีกรอบ (ใช้ตอนเพิ่งเชื่อมครั้งแรก) |
| ปุ่ม **↓ ดึงจากชีต** | ดึงข้อมูลล่าสุดมาทับทันที ไม่ต้องรอ 15 วินาที |
| ปุ่ม **ล้างข้อมูลในเครื่องนี้** | ล้างเฉพาะในมือถือเครื่องนั้น — **ชีตไม่ถูกแตะเลย** กด “↓ ดึงจากชีต” เอากลับมาได้ทันที |

> ไม่มีปุ่มไหนในแอปที่ลบข้อมูลในชีตได้ ถ้าต้องการลบต้องไปลบในชีตเอง

### สถานะบนแถบบนสุด

| ป้าย | ความหมาย |
|---|---|
| ● เชื่อมชีต (เทา) | ยังไม่ได้ตั้งค่า — แตะเพื่อใส่ URL |
| ● กำลังซิงก์ (เหลือง) | กำลังส่ง/ดึงข้อมูล |
| ● ชีตซิงก์แล้ว (เขียว) | ตรงกันกับชีตเรียบร้อย |
| ● ค้าง n / ต่อไม่ติด (แดง) | ส่งไม่ได้ตอนนี้ — เก็บไว้ในเครื่องก่อน แตะเพื่อดูสาเหตุ |

---

## กติกาการเก็บข้อมูล

- เขียนลง **แท็บเดิม** เท่านั้น ไม่สร้างแท็บใหม่ ไม่ย้ายข้อมูลเดิม ไม่สลับลำดับแถว
- **หนึ่งห้อง = หนึ่งแถว** — คีย์คือ `Room No.` อย่างเดียว
  ตรวจห้องเดิมซ้ำเมื่อไหร่ก็ตาม = **ทับแถวเดิมของห้องนั้น** พร้อมอัปเดตวันที่ตรวจให้เป็นวันล่าสุด
- ห้องที่ยังไม่มีในตารางเลย ถึงจะต่อท้ายเป็นแถวใหม่
- แถวที่เตรียมไว้ล่วงหน้า (เช่นห้องที่ใส่ไว้ว่า `รอตรวจ` ยังไม่มีวันที่) พอตรวจจริงจะลงในแถวนั้นเลย
- ถ้ามีแถวซ้ำค้างอยู่ ใช้เมนู **Curtain Check → รวมแถวซ้ำ ให้เหลือห้องละแถว** ครั้งเดียวจบ
  (ยกข้อมูลที่ใหม่สุดขึ้นไปไว้ที่แถวบนสุดของห้องนั้น แล้วลบแถวที่เหลือ ลำดับแถวเดิมไม่เปลี่ยน)
- แอปดูแลคอลัมน์ **A–S** เท่านั้น คอลัมน์ถัดจากนั้น (เช่น `สถานะ`) เป็นของคุณล้วน ๆ แอปไม่แตะ
- **ช่องที่เป็นสูตรจะไม่ถูกเขียนทับ** เช่นถ้า `ผลตรวจ` เป็นสูตร แอปจะปล่อยให้สูตรทำงานต่อ
- แถวใหม่ที่แอปเพิ่ม จะก๊อป **สี / dropdown / สูตร** จากแถวสุดท้ายมาให้อัตโนมัติ หน้าตาเหมือนแถวเดิม
- แก้ค่าในชีตตรง ๆ ได้เลย เดี๋ยวแอปดึงมาแสดงเอง — แต่อย่าสลับลำดับคอลัมน์ A–S

## ถ้าเชื่อมไม่ได้

| อาการ | สาเหตุที่เจอบ่อย |
|---|---|
| กด “ทดสอบ” แล้วขึ้น `Failed to fetch` | ตอน deploy ไม่ได้เลือก **Who has access: Anyone** |
| ขึ้น `ไม่พบแท็บชื่อ …` | `SHEET_NAME` ไม่ตรงกับชื่อแท็บ — ในข้อความ error จะบอกชื่อแท็บที่มีอยู่จริงให้เลย ก๊อปไปวางได้เลย |
| ขึ้น `unauthorized` | `TOKEN` ใน Apps Script กับช่อง Token ในแอปไม่ตรงกัน |
| แก้โค้ดใน Apps Script แล้วไม่มีผล | ต้อง **Deploy → Manage deployments → ✏️ → Version: New version → Deploy** ทุกครั้ง (URL เดิมใช้ต่อได้) |
| อยากเช็คว่าสคริปต์อ่านแท็บไหนอยู่ | ในชีตมีเมนู **Curtain Check → เช็คว่าสคริปต์อ่านแท็บไหน** (ต้อง refresh ชีตหนึ่งครั้งหลังวางโค้ด) |
